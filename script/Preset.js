class Preset {

	constructor(generation, length, angle, axiom, rules, colors) {
		this.generation = generation;
		this.length = length;
		this.angle = angle;
		this.axiom = axiom;
		this.rules = rules;
		this.colors = colors;
	}

}

